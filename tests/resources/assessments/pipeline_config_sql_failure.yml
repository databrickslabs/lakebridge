name: "SQL Failure Pipeline"
version: "1.0"
steps:
  - name: invalid_sql_step
    type: sql
    flag: active
    # Relative to tests/resources/ within the project.
    extract_source: assessments/invalid_query.sql
    mode: overwrite
    frequency: daily

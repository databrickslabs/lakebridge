name: "Python Failure Pipeline"
version: "1.0"
steps:
  - name: invalid_python_step
    type: python
    flag: active
    # Relative to tests/resources/ within the project.
    extract_source: assessments/invalid_script.py
    mode: overwrite
    frequency: daily

name: ExamplePipeline
version: "1.0"
steps:
  - name: random_data
    type: python
    extract_source: db_extract.py
    mode: overwrite
    frequency: daily
    flag: active
    dependencies:
      - pandas
      - duckdb

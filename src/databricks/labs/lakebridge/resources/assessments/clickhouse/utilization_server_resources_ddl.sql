CREATE TABLE IF NOT EXISTS utilization_server_resources (
    total_memory_bytes DOUBLE,
    available_memory_bytes DOUBLE,
    total_memory_gb DOUBLE,
    uptime_seconds DOUBLE,
    fs_total_bytes DOUBLE,
    fs_available_bytes DOUBLE
);

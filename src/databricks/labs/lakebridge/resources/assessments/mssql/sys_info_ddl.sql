CREATE TABLE IF NOT EXISTS sys_info (
    CPU_TICKS BIGINT,
    MS_TICKS BIGINT,
    CPU_COUNT INTEGER,
    HYPERTHREAD_RATIO INTEGER,
    PHYSICAL_MEMORY_KB BIGINT,
    VIRTUAL_MEMORY_KB BIGINT,
    COMMITTED_KB BIGINT,
    COMMITTED_TARGET_KB BIGINT,
    VISIBLE_TARGET_KB BIGINT,
    STACK_SIZE_IN_BYTES INTEGER,
    OS_QUANTUM BIGINT,
    OS_ERROR_MODE INTEGER,
    OS_PRIORITY_CLASS INTEGER,
    MAX_WORKERS_COUNT INTEGER,
    SCHEDULER_COUNT INTEGER,
    SCHEDULER_TOTAL_COUNT INTEGER,
    DEADLOCK_MONITOR_SERIAL_NUMBER INTEGER,
    SQLSERVER_START_TIME_MS_TICKS BIGINT,
    SQLSERVER_START_TIME TIMESTAMP,
    AFFINITY_TYPE SMALLINT,
    AFFINITY_TYPE_DESC VARCHAR,
    PROCESS_KERNEL_TIME_MS BIGINT,
    PROCESS_USER_TIME_MS BIGINT,
    TIME_SOURCE INTEGER,
    TIME_SOURCE_DESC VARCHAR,
    VIRTUAL_MACHINE_TYPE INTEGER,
    VIRTUAL_MACHINE_TYPE_DESC VARCHAR,
    SOFTNUMA_CONFIGURATION SMALLINT,
    SOFTNUMA_CONFIGURATION_DESC VARCHAR,
    PROCESS_PHYSICAL_AFFINITY VARCHAR,
    SQL_MEMORY_MODEL INTEGER,
    SQL_MEMORY_MODEL_DESC VARCHAR,
    SOCKET_COUNT INTEGER,
    CORES_PER_SOCKET INTEGER,
    NUMA_NODE_COUNT INTEGER,
    CONTAINER_TYPE INTEGER,
    CONTAINER_TYPE_DESC VARCHAR,
    EXTRACT_TS TIMESTAMP
);
